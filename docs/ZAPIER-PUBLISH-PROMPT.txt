Fix this existing Zap. Do not add a Code step or ask me for document IDs or timestamps.

Keep step 1: Writesonic - New Copy Published.
In step 2, Webhooks by Zapier - POST:
- Keep URL https://trophy.guru/api/webhooks/writesonic
- Keep Payload Type JSON.
- Keep the existing Authorization and Content-Type headers unchanged.
- Replace the Data mapping with exactly two fields:
  mode: publish (literal text)
  text: the dynamic text output from step 1 (the full article, not a typed placeholder).
- Wrap Request In Array: No. Unflatten: No.
- Remove documentId, updatedAt, title, contentHtml and other obsolete mappings if present.

The site now accepts the text field and derives the article title and URL. No additional fields or transformation steps are needed.
Use an original real Writesonic article record containing its title and full body, not a manually modified sample.
Testing step 2 in publish mode publishes that article. If only placeholder records are available, save the mapping without sending a placeholder to the site.
Publish/enable the configured Zap when Zapier permits it. Report its actual state and any error accurately.
